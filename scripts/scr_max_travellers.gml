tmp_max_counter = 0;
for(tmp_t = 1; tmp_t < travellerName.lastChar; tmp_t++)
    if traveller_pick[tmp_t]
        tmp_max_counter++;
if tmp_max_counter >= side_chars_max
    return true;
else
    return false;
