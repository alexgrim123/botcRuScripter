tmp_max_counter = 0;
for(tmp_t = 0; tmp_t < fabledName.lastChar; tmp_t++)
    if fabled_pick[tmp_t]
        tmp_max_counter++;
if tmp_max_counter >= side_chars_max
    return true;
else
    return false;
